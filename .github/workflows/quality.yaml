name: Quality

on:
  pull_request:
  push:
    branches: [main]

jobs:
  quality:
    uses: significa/actions/.github/workflows/typescript-quality.yaml@main
    with:
      typecheck_command: "echo 'no typecheck — configs repo'"
      knip_command: "echo 'no knip — configs repo'"
